.venv/
__pycache__/
*.pyc
*.pyo
.DS_Store
.pytest_cache/

tool-*.json

runtime/
configs/*.json
!configs/example.json

tools/

*.env
.env
# keep example files
!deploy/*.env.example
!deploy/*.service.example

# local scratch files
chat.txt
docs/plan-*.md

# Claude Code's local settings: the allow-list quotes whole curl commands,
# tokens and internal addresses included. It was only kept out by
# .git/info/exclude, which lives outside the repo and does not survive a
# fresh clone — so the rule belongs here, where it travels with the code.
.claude/
