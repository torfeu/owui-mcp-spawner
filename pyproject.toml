[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "owui-mcp-spawner"
# Single source of truth: app/__init__.py:__version__
dynamic = ["version"]
description = "Spawn OpenWebUI tool JSONs as isolated Model Context Protocol (MCP) servers"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.27.0",
    "pydantic>=2.0.0",
    "python-multipart>=0.0.9",
    "mcp>=2.0.0",  # the runner speaks the 2.x low-level Server API
    "packaging>=24.0",
    "starlette>=0.36.0",
    "httpx>=0.27.0",
    "psutil>=5.9.0",  # system monitor; imported defensively so an old venv still boots
]

[tool.setuptools.dynamic]
version = {attr = "app.__version__"}

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
